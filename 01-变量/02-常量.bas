Sub MainSub()

    ' =============================
    ' 常量特点
    '   一旦定义，不能修改
    '   必须在定义时赋值
    '   通常用大写（规范，不强制）
    ' =============================
    Const PI As Double = 3.14159
    Const MAX_COUNT As Long = 100
    Const GREETING As String = "Hello"

End Sub